Change the Industrial, Municipal & Utility services overview page from a two-column card layout into full-width horizontal rows, matching the Private Lands services page.

PAGE TO UPDATE

/industrial-municipal-utility/index.html

GOAL

Make every industrial service appear as a wide horizontal row instead of a box or square card.

Use one service per row on desktop and mobile.

FINAL SERVICE ORDER

1. Recurring Vegetation Maintenance Programs
2. Industrial and Municipal Vegetation Management
3. Drainage and Detention Vegetation Management
4. Rights-of-Way and Site Perimeters
5. Mechanical Vegetation Management
6. Herbicide and Chemical Vegetation Control
7. Vegetation Mapping and Reporting
8. Industrial Wildlife Management
9. Invasive Species Control

LAYOUT

Replace the current two-column grid with one column.

Use:

.industrial-services-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 24px;
}

Each service row should:

- span the full available content width
- use a wide rectangular format
- avoid fixed heights
- grow naturally with the content
- use consistent padding
- use the same visual treatment as the Private Lands service rows
- keep the title, description, and button aligned consistently
- keep readable spacing between rows

CARD / ROW STYLE

Use the same service-row styling already applied on the Private Lands overview page.

If shared classes can be reused safely, use them instead of creating duplicate CSS.

Preferred appearance:

- warm-white background
- charcoal text
- subtle gold top border
- soft shadow
- restrained rounded corners
- generous horizontal padding
- no image
- no gradient

Use:

.industrial-services-grid .service-card {
  width: 100%;
  max-width: none;
  min-height: 0;
  background: #FAF9F5;
  color: #252A27;
  border-top: 3px solid #B58A48;
  border-left: 1px solid #D8D3C7;
  border-right: 1px solid #D8D3C7;
  border-bottom: 1px solid #D8D3C7;
  padding: 32px 44px;
  box-shadow: 0 10px 24px rgba(0,0,0,0.12);
}

.industrial-services-grid .service-card p {
  max-width: 900px;
}

BUTTONS

On desktop:

- keep buttons aligned beneath the description
- do not stretch buttons across the full row unless that matches the Private Lands design
- use consistent button width

On mobile:

- make buttons full-width

MOBILE

At 768px and below:

- keep one service row per screen section
- use 20px card padding
- use 16px to 18px page padding
- make rows full-width
- make buttons full-width
- keep titles from breaking in the middle of words
- preserve current mobile description shortening
- prevent horizontal scrolling

Use:

@media (max-width: 768px) {
  .industrial-services-grid {
    grid-template-columns: 1fr !important;
  }

  .industrial-services-grid .service-card {
    width: 100% !important;
    padding: 22px 20px !important;
  }

  .industrial-services-grid .service-card a[role="button"],
  .industrial-services-grid .service-card .button {
    width: 100% !important;
    min-width: 0 !important;
  }
}

REMOVE CONFLICTING RULES

Remove or override:

- two-column grid rules
- repeat(2, ...) grid rules
- square-card sizing
- fixed card heights
- special span rules
- duplicate desktop and mobile service lists

Keep only one shared list of nine service rows.

Do not:

- change service names
- change service descriptions
- change links
- change button labels
- change the service order
- change page background
- add images
- use gradients
- alter the Private Lands page
- run screenshot verification
- analyze screenshots
- start browser automation
- run appearance-check loops

Save and stop immediately.

Report only:

- file changed
- industrial layout changed to one column
- total number of service rows
- mobile layout preserved
- final service order