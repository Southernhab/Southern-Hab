Fix only the mobile layout on:

/industrial-municipal-utility/index.html

CURRENT PROBLEM

The heading “Industrial, Municipal and Utility Services” is wrapping into narrow broken lines on mobile.

MOBILE FIX

At screen widths of 768px and below:

1. Make the page intro container use the full available width.
2. Remove any narrow fixed-width column.
3. Remove any max-width that is too small.
4. Remove any grid column that squeezes the heading.
5. Left-align the heading.
6. Keep normal word wrapping.
7. Do not allow words to break in the middle.

Use these rules:

@media (max-width: 768px) {
  .page-hero,
  .overview-hero,
  .intro-section,
  .intro-copy,
  .page-intro {
    width: 100% !important;
    max-width: 100% !important;
    min-width: 0 !important;
    display: block !important;
  }

  .page-hero h1,
  .overview-hero h1,
  .intro-section h1,
  .page-intro h1 {
    width: 100% !important;
    max-width: 100% !important;
    min-width: 0 !important;
    font-size: clamp(32px, 8vw, 40px) !important;
    line-height: 1.08 !important;
    white-space: normal !important;
    overflow-wrap: normal !important;
    word-break: normal !important;
    hyphens: none !important;
  }

  .page-hero,
  .overview-hero,
  .intro-section,
  .page-intro {
    padding-left: 18px !important;
    padding-right: 18px !important;
  }
}

REMOVE CONFLICTING RULES

Remove or override any mobile rule that applies:

- narrow grid columns
- max-width below 100%
- width values such as 25%, 30%, or fixed pixel widths
- word-break: break-all
- overflow-wrap: anywhere
- vertical writing modes
- flex-basis values that squeeze the heading
- absolute positioning on the heading
- fixed desktop columns that remain active on mobile

Keep unchanged:

- desktop layout
- page copy
- service cards
- links
- buttons
- forms
- colors
- fonts
- navigation
- footer

Do not run screenshot verification, browser automation, or appearance-check loops.

Save the affected stylesheet or page-specific style and stop immediately.