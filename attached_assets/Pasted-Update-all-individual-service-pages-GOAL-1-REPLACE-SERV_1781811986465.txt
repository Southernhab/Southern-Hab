Update all individual service pages.

GOAL 1: REPLACE “SERVICE DESCRIPTION”

On every service page, replace the generic heading:

“Service Description”

with the actual service name for that page.

Examples:

- Forestry and Timber Management
- Habitat Management
- Prescribed-Fire Planning and Management
- Industrial Vegetation Management
- Municipal Vegetation Management
- Mapping and Reporting

Use the existing page title or breadcrumb to determine the correct service name.

Do not use “Service Description” anywhere on the site.

GOAL 2: REMOVE THE LARGE EMPTY HERO BLOCK

On every individual service page, remove the oversized empty dark-green section shown above the service content.

Remove:
- the large empty vertical space
- the oversized dark-green background block
- any excessive minimum-height
- any large top padding or bottom padding creating the empty area
- any placeholder hero area with no useful content

Keep the service-page header compact.

The top of each service page should contain only:

1. Breadcrumb or back link
2. Actual service name
3. Optional short introductory paragraph, if one already exists

Use a light background consistent with the rest of the site:

- Background: #FAF9F5 or #F3EFE4
- Heading color: #1F3A2E
- Body text: #252A27
- Border: #D8D3C7

Do not use:
- dark-green empty blocks
- gradients
- blue text
- oversized minimum heights
- excessive blank space

SPACING

Use:
- top and bottom padding around 48px to 72px on desktop
- top and bottom padding around 32px to 48px on mobile
- no empty space taller than the visible content
- normal spacing between breadcrumb, heading, and body text

KEEP UNCHANGED

Do not change:
- approved service copy
- inquiry links
- related-service links
- forms
- navigation
- footer
- page URLs
- fonts
- color palette outside the service-page header area

Do not:
- run screenshot verification
- analyze screenshots
- start browser automation
- run appearance-check loops

Apply this across all Industrial, Municipal & Utility service pages and all Private-Land service pages.

Stop immediately after updating the service-page headings and removing the empty hero blocks.

Report only:
- files changed
- pages where “Service Description” was replaced
- pages where oversized empty blocks were removed