Paste this into Replit Agent:

```text
Update the service-card layout so every service box matches the wide rectangular style of the Forestry Services card.

PAGE TO UPDATE

/private-land-services/index.html

CURRENT PROBLEM

Most service cards are narrow and square-like, while the Forestry Services card is wider and easier to read.

DESIGN CHANGE

Make every service card a wide horizontal rectangle.

Use one service card per row on desktop and mobile.

Each card should:

- span the full available content width
- use a horizontal rectangular shape
- have the same width and general styling as the Forestry Services card
- use consistent padding
- use the same border treatment
- use the same background color
- use the same heading size
- use the same button style
- avoid fixed heights
- grow naturally based on content

DESKTOP LAYOUT

At screen widths above 768px:

- use one column
- each service card should span 100% of the content area
- remove multi-column card grids
- remove special two-column span rules from Forestry Services
- make all cards visually equal in width
- keep comfortable spacing between cards
- keep descriptions readable and not overly wide

Use:

.private-services-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 24px;
}

.private-services-grid .service-card {
  width: 100%;
  max-width: none;
  min-height: 0;
}

CARD STRUCTURE

Inside each card:

- keep the service title at the top
- place the description below it
- place the Learn More button below the description
- keep the button from stretching unnecessarily on desktop
- align content consistently
- do not force equal card heights

Preferred styling:

.service-card {
  background: #FAF9F5;
  color: #252A27;
  border-top: 3px solid #B58A48;
  border-left: 1px solid #D8D3C7;
  border-right: 1px solid #D8D3C7;
  border-bottom: 1px solid #D8D3C7;
  padding: 32px 44px;
  box-shadow: 0 10px 24px rgba(0,0,0,0.12);
}

.service-card p {
  max-width: 850px;
}

.service-card a[role="button"],
.service-card .button {
  width: auto;
  min-width: 220px;
}

MOBILE

At 768px and below:

- keep one card per row
- use full-width cards
- use 20px padding
- make buttons full-width
- keep card descriptions limited to 2–3 lines only if the existing mobile line-clamp is already being used
- prevent headings from breaking in the middle of words
- remove fixed widths and min-width values
- prevent horizontal scrolling

Use:

@media (max-width: 768px) {
  .service-card {
    width: 100% !important;
    padding: 22px 20px !important;
  }

  .service-card a[role="button"],
  .service-card .button {
    width: 100% !important;
    min-width: 0 !important;
  }
}

CONSISTENCY

Apply this same wide rectangular card treatment to every card on the Private Lands overview page, including:

- Property Assessment and Planning
- Forestry Services
- Habitat Management
- Prescribed Fire Planning
- Wildlife Management
- Private Land Invasive Plant Control
- Complete Property Management
- Property Mapping, Monitoring and Records

Do not:

- change service names
- change descriptions
- change links
- change button labels
- change page background
- add images
- use gradients
- alter other pages
- run screenshot verification
- analyze screenshots
- start browser automation
- run appearance-check loops

Save and stop immediately.

Report only:

- file changed
- card grid changed to one column
- all service cards made full-width
- mobile button behavior preserved
```

This will make every card match the longer Forestry Services format instead of appearing as square tiles.
