Edit only /index.html.

Do not inspect or modify any other file.

Immediately before the closing </head> tag, add this Google Fonts link:

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&family=Source+Sans+3:wght@400;500;600;700&display=swap" rel="stylesheet">

Then add this exact style block immediately after those links:

<style id="sitewide-visual-override">
  :root {
    --forest: #1F3A2E;
    --pine: #13271F;
    --cream: #F3EFE4;
    --warm-white: #FAF9F5;
    --charcoal: #252A27;
    --moss: #66765A;
    --gold: #B58A48;
    --border: #D8D3C7;
  }

  html,
  body {
    background-color: var(--warm-white) !important;
    color: var(--charcoal) !important;
    font-family: "Source Sans 3", Arial, sans-serif !important;
  }

  h1,
  h2,
  h3,
  h4 {
    color: var(--forest) !important;
    font-family: "Libre Baskerville", Georgia, serif !important;
  }

  p,
  li,
  a,
  label,
  input,
  select,
  textarea,
  button,
  nav,
  footer {
    font-family: "Source Sans 3", Arial, sans-serif !important;
  }

  nav {
    background-color: var(--warm-white) !important;
    color: var(--charcoal) !important;
  }

  nav a {
    color: var(--charcoal) !important;
  }

  article,
  .card,
  form {
    background-color: var(--cream) !important;
    border-color: var(--border) !important;
  }

  button,
  .button,
  a[role="button"] {
    background-color: var(--forest) !important;
    border-color: var(--forest) !important;
    color: var(--warm-white) !important;
    text-transform: none !important;
    letter-spacing: 0 !important;
  }

  button:hover,
  .button:hover,
  a[role="button"]:hover {
    background-color: var(--pine) !important;
    border-color: var(--pine) !important;
  }

  footer {
    background-color: var(--pine) !important;
    color: var(--warm-white) !important;
  }

  footer a {
    color: var(--warm-white) !important;
  }
</style>

Do not:
- create a stylesheet
- plan a stylesheet
- change copy
- change layout
- change links
- change forms
- run screenshots
- run browser testing
- verify appearance
- inspect other pages

Save /index.html and stop immediately.