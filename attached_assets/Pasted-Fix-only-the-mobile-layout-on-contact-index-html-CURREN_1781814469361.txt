Fix only the mobile layout on:

/contact/index.html

CURRENT PROBLEM

The “Industrial or Municipal” form heading is being squeezed into a narrow column and breaking words across multiple lines.

MOBILE FIX

At screen widths of 768px and below:

1. Make the contact form container use the full available width.
2. Remove any narrow fixed-width column.
3. Remove any grid layout that keeps the form heading beside another element.
4. Stack the form selector and form content vertically.
5. Keep the form heading on normal lines.
6. Do not break words in the middle.
7. Keep all fields full-width.

Use these rules:

@media (max-width: 768px) {
  .contact-page,
  .contact-section,
  .contact-wrapper,
  .form-shell,
  .inquiry-form,
  .form-panel,
  .form-container {
    width: 100% !important;
    max-width: 100% !important;
    min-width: 0 !important;
    display: block !important;
  }

  .form-grid,
  .contact-grid,
  .inquiry-grid,
  .form-layout {
    display: grid !important;
    grid-template-columns: 1fr !important;
    width: 100% !important;
  }

  .form-shell h2,
  .form-shell h3,
  .inquiry-form h2,
  .inquiry-form h3,
  .form-panel h2,
  .form-panel h3 {
    width: 100% !important;
    max-width: 100% !important;
    min-width: 0 !important;
    font-size: clamp(28px, 8vw, 34px) !important;
    line-height: 1.15 !important;
    white-space: normal !important;
    overflow-wrap: normal !important;
    word-break: normal !important;
    hyphens: none !important;
  }

  .form-shell,
  .inquiry-form,
  .form-panel {
    padding: 20px !important;
  }

  label,
  input,
  select,
  textarea,
  button {
    width: 100% !important;
    max-width: 100% !important;
  }

  input,
  select,
  textarea {
    font-size: 16px !important;
  }
}

CONTACT FORM SELECTOR

On mobile, use a simple dropdown or stacked selector for:

- Private Land Inquiry
- Industrial / Municipal Inquiry

Show only one form at a time.

Do not place both forms side by side.

Keep both forms in the HTML and keep their submission behavior unchanged.

REMOVE CONFLICTING RULES

Remove or override any mobile rule that applies:

- fixed-width form columns
- side-by-side form layout
- width values below 100%
- narrow grid columns
- word-break: break-all
- overflow-wrap: anywhere
- vertical writing modes
- fixed heading widths
- absolute positioning on the form heading

Keep unchanged:

- desktop layout
- form fields
- form destinations
- form validation
- page copy
- colors
- fonts
- navigation
- footer

Do not run screenshot verification, browser automation, or appearance-check loops.

Save the affected stylesheet or page-specific style and stop immediately.